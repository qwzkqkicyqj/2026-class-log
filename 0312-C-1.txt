#include <stdio.h>
int main(){
    int a;
    while(1){ //참일때 반복, 0->거짓(false), 1->참(True)
              //무한반복
              
    printf("숫자를 입력하세요.: ");
    scanf("%d", &a);
    if (a==10){
        printf("정답입니다.\n\n");
        break;
    } else if (a>10) {
        printf("너무 높아요.\n\n");
    } else {
        printf("너무 낮아요.\n\n");
    }
    }
    return 0;
}